#include "holberton.h"

/**
 * _abs - Computes the absolute value of an int
 * @x: generates absolute value
 * Return: absolute value
 */

int _abs(int x)
{
	if (r > 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
}
