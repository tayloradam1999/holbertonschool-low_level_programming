#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, followed be a new line
 *
 * Return: 0!
 *
 */

int main(void)
{
	for(int x = 'A'; x <= 'Z'; x++)
	{
		x = tolower(x);
		putchar(x);
	{
	return 0;
}

